# -*- coding: utf-8 -*-
"""退出码：**单一来源**（T8 的「rc 语义一致性」由静态规则 R7 守）。

为什么把几个数字单独放一个文件：退出码是最容易被各处随手写死的东西——
`return 3` 在 A 处是「落后」，在 B 处是「自检不过」，调用方（计划任务、CI、
守护脚本）就没法判定。v1 的现场就是「靠人记哪个数字是什么意思」。

约定：**0 成功**；1 参数/用法错误；2 规则或自检未过；3 落后于最新发布或升级后自检未过；
4 严格模式下拿不到最新版（或环境不满足）；124/127 沿用 shell 惯例（超时/找不到命令）。

新增退出码＝先在这里加常量，再去用它——`cli.py` 里出现裸整数（≥2）会被 R7 判 FAIL。
"""
from __future__ import annotations

OK = 0                # 成功
USAGE = 1             # 参数或用法错误
RULES_FAIL = 2        # 静态规则 / 自检未过
BEHIND = 3            # 落后于最新发布（或升级后自检未过，已回滚）
REFUSE = 4            # 严格模式（--require-latest）下未能拿到最新版；或环境不满足
TIMEOUT = 124         # 执行者超时（shell 惯例）
NOT_FOUND = 127       # 执行者命令找不到（shell 惯例）

MEANING = {
    OK: "成功",
    USAGE: "参数或用法错误",
    RULES_FAIL: "静态规则或自检未过",
    BEHIND: "落后于最新发布（或升级后自检未过）",
    REFUSE: "严格模式：未能拿到最新版；环境不满足",
    TIMEOUT: "执行者超时",
    NOT_FOUND: "执行者命令找不到",
}
