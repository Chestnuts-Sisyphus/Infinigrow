# -*- coding: utf-8 -*-
"""账本层：追加型账本 + 工作文件的原子写。

两条纪律（v1 现场用事故换来的）：

1. **账本只增不改**（`*.jsonl` 追加）——差异账、兑现账、成熟链、能力库都是「事实记录」，
   一旦可改，统计就能被顺手改绿。合并/冻结只动**队列**（工作文件），账本全留。
2. **工作文件可覆写，但必须过标记闸**：覆写前要求新文本含约定标记（如 `## 芽队列`），
   防止「一次截断把半个状态文件洗掉」——v1 出过截断事故，于是有了安全写盘唯一入口。
"""
