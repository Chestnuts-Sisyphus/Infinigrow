# -*- coding: utf-8 -*-
"""机械园丁：免疫系统。不烧认知（零 token），只管「机器能判的死活」。

管四件事：
  1. **死锁扫描**：`locks/` 里的陈旧锁清掉（判据＝文件 mtime 年龄，机械时间戳）；
  2. **断流**：最后一拍距今超过阈值 → 置致命旗（判据＝机械时间戳，不用「模型自述」）；
  3. **失败升级**：连续失败 ≥3 拍 → 置致命旗；
  4. **完整性**：状态目录关键文件是否还在、账本有没有坏行。

致命旗落在 `state/ALERT.md`——**给人看的**（人不需要天天读，但出事时必须有据可查）。
"""
